{% extends 'base.html.twig' %}

{% block content %}
    <h1>Inscription</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-primary" value="S'inscrire">
    {{ form_end(form) }}
{% endblock %}
